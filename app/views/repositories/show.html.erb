<%= render "shared/files" %>

<p><%= @commit.message %> - <%= @commit.committer[:name] %> (<%= @commit.committer[:email] %>)</p>

<h3>Branches</h3>
<% @repo.branches.each do |branch| %>
  <% if branch.target.type == :commit %>
    <li><%= branch.name %> (<%= branch.target.message %>)</li>
  <% end %>
<% end %>
